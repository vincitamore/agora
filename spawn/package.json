{
  "name": "agora-pane",
  "version": "0.1.0",
  "description": "The pane authority: owns every Bun.Terminal. The root CLI never imports this package.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "bun run listen.ts",
    "test": "bun test",
    "check": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
