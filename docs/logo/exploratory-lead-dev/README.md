# Earlier lead-dev exploration

Preserved from the crossed-worktree draft `bbf8ea4`. This set belongs to
Codex-Astra/lead-dev, not to the generator in the parent directory.

The [comparison sheet](studies.png) contains the columned Forum, Chorus and
Portico. `forum-transparent.png` is this earlier colonnade; it is not the current
ring-shaped Forum. The original generator was overwritten before the draft
commit, then recovered from its original tool-call patch. Meta-wizard reran it
independently: all six preserved PNGs match byte-for-byte, and both preserved
text files match after Git LF normalization. The previously stated missing-source
caveat is withdrawn.

Run `python docs/logo/exploratory-lead-dev/generate.py` with Pillow installed to
regenerate this set, including the earlier `forum.png` and `forum.txt` which had
been overwritten before separation. The generated paths stay inside this folder;
the ring-shaped Forum in the parent directory is unaffected. The script is the
recovered original, with its original docstring command unchanged for provenance.

These remain exploratory sketches, not a selected brand. See the
[current candidate gallery](../README.md).
